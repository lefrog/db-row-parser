{
  "name": "db-row-parser",
  "version": "2.1.0",
  "description": "A simple module to convert row (array) from result of SQL query or from a csv parser to an object.",
  "main": "index.js",
  "scripts": {
    "test": "node_modules/.bin/mocha 'specs/**/*.js'"
  },
  "keywords": [
    "Database",
    "csv",
    "row",
    "parser",
    "object",
    "mapping",
    "inflate"
  ],
  "author": "Pascal Lambert",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/lefrog/db-row-parser.git"
  },
  "devDependencies": {
    "mocha": "^2.3.4",
    "through2": "^2.0.0"
  }
}
